<% unless (@breadcrumb_items ||= []).empty? %>
  <div class="breadcrumb-holder container-fluid">
    <ul class="breadcrumb">
      <% @breadcrumb_items.each do |item| %>
        <%= render "components/breadcrumb/item", item %>
      <% end %>
    </ul>
  </div>
<% end %>
